
<%= form_for([@board, @board.comments.build]) do |f| %>
  <div class="field">
    <%= f.label "comment" %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
